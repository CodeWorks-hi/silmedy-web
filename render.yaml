# render.yaml (프로젝트 루트에 위치)
services:
  - type: web
    name: silmedy-web
    env: node
    plan: free           # 무료 플랜
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    # envVars 블록을 비워두거나 완전히 제거합니다.
    # 대신 Render 대시보드에서 직접 key/value 쌍을 설정하세요.